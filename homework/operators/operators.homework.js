/*
* Using a common theme (pick whatever you like) demonstrate two of the
* following categories of operators:
*
* - Arithmetic Operators (+, -, *, /)
* - Comparison Operators (===, !==, >, <, >=, <=)
* - Logical Operators (&&, ||, !)
* - String Operator (+)
*
* You will need to create variables to hold values and then log some
* sort of output to the console that demonstrates the use of these operators.
*
* Example:
*    let peasantHealth = 50;
*    const wolfDamage = 20;
*    peasantHealth = peasantHealth - wolfDamage;
*    console.log('The peasant has', peasantHealth, 'health left after the wolf attack.');
*/

// Begin writing your code below

let quiver = 54 + 45;
console.log("It seemed like a good number to me, and Scratch usually gets annoyed if I add more, so the max quiver turned out to be", quiver, "in the end.");
let coins = 25 * 10;
console.log("My original plan had been 999 coins as a max wallet but Scratch wasn't too happy about having to deal with all that, so instead I had it max out at", coins,"coins.");
let characterProfile = "Lyn" + " is" + " a" + " spirit" + " and" + " is" + " skilled" + " at" + " archery.";
console.log(characterProfile);